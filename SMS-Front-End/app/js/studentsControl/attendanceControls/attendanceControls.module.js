// A module without dependencies, with name "teachers"
angular.module('attendanceControls', ['md.data.table'])
    .config(function(){
        console.log('Activating attendanceControls module.');

      })

